---
interface Props {
  userVote: number | null | undefined;
}

const { userVote } = Astro.props;
---

<span
  class=`knk-notch-sm px-2.5 py-1 text-sm font-medium border tabular-nums knk-vote-badge transition-colors knk-vote-${userVote}`
  title="Tu voto"
>
  Tu puntuación: {userVote}
</span>

export interface GameCardEls {
  root: HTMLElement;
  toggle: HTMLButtonElement;
  picker: HTMLElement;
  avgBadge: HTMLElement | null;
  avgLabel: HTMLElement | null;
  voteCount: HTMLElement | null;
  personalBadge: HTMLElement | null;
}
